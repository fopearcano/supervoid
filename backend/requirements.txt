fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlmodel==0.0.22
alembic==1.13.3
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
bcrypt==4.2.0
PyJWT==2.9.0
python-multipart==0.0.12
httpx==0.27.2
# PostgreSQL + pgvector: production-only (cold-detail retrieval, Prompt 14).
# Lazily imported by the pgvector vector backend, so the SQLite test suite and
# air-gapped dev never need them installed.
psycopg[binary]==3.2.3
pgvector==0.3.6
